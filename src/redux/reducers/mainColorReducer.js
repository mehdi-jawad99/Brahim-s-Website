import types from "../actions/types";

const initialState = {
  color: "#2085AA",
  second: "#115D78",
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
